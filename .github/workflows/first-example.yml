name: My First Workflow

on: push

jobs:
  firt_job:
    runs-on: ubuntu-latest
    steps:
    - name: Welcome message
      run: echo "My first Github Actions Job"
    
    - name: list files
      run: ls

    - name: read files
      run: pwd